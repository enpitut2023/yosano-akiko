{
  "name": "akiko",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "live-server": "live-server --port=42263 public"
  },
  "devDependencies": {
    "@types/node": "^24.9.1"
  }
}
